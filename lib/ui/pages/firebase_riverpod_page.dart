import 'package:flutter/material.dart';

class FirebaseRiverpodPage extends StatelessWidget {
  const FirebaseRiverpodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Firebase & Riverpod')),
      body: const Center(child: Text('Firebase & Riverpod Page')),
    );
  }
}
