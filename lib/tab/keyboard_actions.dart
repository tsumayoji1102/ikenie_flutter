import 'package:flutter/material.dart';

class KeyboardActionsView extends StatelessWidget {
  const KeyboardActionsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Keyboard'),
    );
  }
}
